import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function ActivitiesPage() {
  return (
    <PlaceholderPage
      title="Activities"
      description="A future activity master for workshops, judging sessions, briefings, practice blocks, and scheduled units."
      nextSteps={[
        "Model activities under a selected competition.",
        "Add reusable activity create and edit components.",
        "Prepare participant assignment workflows.",
      ]}
    />
  );
}
