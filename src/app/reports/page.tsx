import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function ReportsPage() {
  return (
    <PlaceholderPage
      title="Reports"
      description="A future reporting area for exports, summaries, and printable planning outputs."
      nextSteps={[
        "Add competition and season filters.",
        "Use summarized data for charts.",
        "Add export actions after core workflows exist.",
      ]}
    />
  );
}
